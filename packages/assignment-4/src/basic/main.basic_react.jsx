import React from 'react';
import ReactDOM from 'react-dom/client';
import Main from '../main_react';

ReactDOM.createRoot(document.getElementById('app')).render(
  <React.StrictMode>
    <Main />
  </React.StrictMode>
);
